# Development and CI dependencies for the rocketdoo repo itself.
# Runtime deps live in pyproject.toml [project.dependencies].
#
#   pip install -e . -r requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
httpx>=0.27        # FastAPI TestClient transport
